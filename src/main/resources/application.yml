spring:
  application:
    name: odos-server-v2

  datasource:
    url: jdbc:mysql://localhost:3306/odos2
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: 11111111

  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    open-in-view: false
    show-sql: true
    hibernate:
      format_sql: true
      ddl-auto: update